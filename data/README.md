# Data

This directory is for XCT data or pre-generated network files.

The Khan et al. (2021) paper uses NREL XCT data for NMC532 cathodes.
If the original data is not available, use synthetic networks generated
by `scripts/generate_network.py`.
